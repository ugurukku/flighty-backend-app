spring:
  application:
    name: ms-flight
  cloud:
    config:
      profile: native
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      name: flight
      label: ms

  profiles:
    active: native